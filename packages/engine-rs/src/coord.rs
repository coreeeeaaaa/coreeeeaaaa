#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
pub struct Coord9 {
    pub t: u64,
    pub x: u64,
    pub a: u32,
    pub w: u32,
    pub j: u64,
    pub k: u32,
    pub p: u8,
    pub m: u8,
    pub c: u8,
}
